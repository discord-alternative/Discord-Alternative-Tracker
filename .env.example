# Cloudflare credentials for non-interactive use (CI, scripted deploys).
# Local development does not need these: run `npm run login` once instead.
# Copy this file to .env and fill in the values. .env is ignored by git.
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
